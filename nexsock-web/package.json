{
  "name": "nexsock-web",
  "version": "1.0.0",
  "description": "Web interface for Nexsock service management",
  "scripts": {
    "build": "bun build src-ts/main.ts --outdir=public/js --target=browser --format=iife --minify --sourcemap --outfile=public/js/main.min.js",
    "build:dev": "bun build src-ts/main.ts --outdir=public/js --target=browser --format=iife --sourcemap --outfile=public/js/main.min.js",
    "watch": "bun build src-ts/main.ts --outdir=public/js --target=browser --format=iife --sourcemap --outfile=public/js/main.min.js --watch",
    "type-check": "bun x tsc --noEmit",
    "install": "bun install"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "htmx.org": "^1.9.8"
  }
}